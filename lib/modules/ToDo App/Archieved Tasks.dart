import 'package:flutter/material.dart';

class ArchievedTasks extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Archieved Tasks',
        style: TextStyle(
          fontSize: 30,
          fontWeight: FontWeight.bold,
        ),
      ),
    );
  }
}
